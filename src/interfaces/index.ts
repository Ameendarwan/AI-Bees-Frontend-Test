export interface ArrayObjectsProps {
  id: number | string;
  title: string;
  description: string;
  priority: string;
}

export interface ObjectProps {
  id: number | string;
  title: string;
  description: string;
  priority: string;
}
